<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/background_color"
        tools:context=".view.fragments.WeekFragment">

        <ProgressBar
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:id="@+id/progress_bar"
            android:padding="@dimen/_100sdp"
            android:visibility="gone"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"/>

        <androidx.appcompat.widget.Toolbar
            android:id="@+id/week_toolbar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:title="@string/week_forecast"
            android:textAlignment="center"
            android:background="@color/app_bar_color"
            app:titleTextColor="@color/app_bar_text"
            app:layout_constraintTop_toTopOf="parent" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recycler_view"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            tools:listitem="@layout/week_weather_layout"
            android:layout_margin="@dimen/_5sdp"
            android:maxLength="5"
            app:layout_constraintTop_toBottomOf="@id/week_toolbar"/>

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>